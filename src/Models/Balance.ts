export class Balance {
  public balance: number;
  public income: number;
  public expenses: number;

  constructor(balance: number, income: number, expenses: number) {
    this.balance = balance;
    this.income = income;
    this.expenses = expenses;
  }
}
